---
const { class: klass = "", ...rest } = Astro.props;
---

<a
    class:list={[
        // base
        "inline-flex shrink-0 items-center justify-center gap-x-2 rounded-full",
        "border border-white/10 bg-white/10 hover:bg-white/5 hover:scale-110",
        "py-1 px-2 md:py-2 md:px-4 transition duration-200",
        "text-xs md:text-base whitespace-nowrap",
        // extra desde el uso
        klass,
    ]}
    {...rest}
    target="_blank"
    rel="noopener noreferrer"
>
    <slot />
</a>
